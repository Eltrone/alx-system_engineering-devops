e script configure Nginx pour ajouter un en-tête HTTP personnalisé X-Served-By

# Installation de Nginx si ce n'est pas déjà fait
sudo apt-get update
sudo apt-get install -y nginx

# Configuration de Nginx pour ajouter l'en-tête personnalisé
HOSTNAME=$(hostname)
sudo sed -i "/server_name _;/a \\tadd_header X-Served-By $HOSTNAME;" /etc/nginx/sites-available/default

# Redémarrage de Nginx pour appliquer les modifications
sudo systemctl restart nginx
