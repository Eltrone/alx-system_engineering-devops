#!/usr/bin/env bash
# Script that configures Nginx to include custom HTTP response header

# Install Nginx
sudo apt-get update
sudo apt-get -y install nginx

# Configure custom HTTP response header
echo "add_header X-Served-By \$hostname;" | sudo tee -a /etc/nginx/sites-available/default
sudo systemctl restart nginx

